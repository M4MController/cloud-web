{
    host: 'localhost',
    port: 5000,

    user_token: "admin12345",

    database: {
        name: "mongo_db", # Database name
        host: "localhost",
        port: 27017,

        # Initial values to store in db
        initials: [
            { id: 7, name: "gps", file: "./gps.json" },
            { id: 8, name: "obd",  file: "./obd.json" }
        ]
    }
}