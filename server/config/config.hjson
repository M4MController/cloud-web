{
    host: 'localhost',
    port: 5000,

    user_token: "admin12345",

    database: {
        name: "mongo_db", # Database name
        host: "localhost",
        port: 27017,

        # Initial values to store in db
        initials: [
            { id: 7, file: "./gps.json" },
            { id: 8, file: "./obd.json" }
        ]
    }
}